from parser import TOKENS
from '_pyscript>mapping' import SYMBOLS_TOKEN_MAP

EXACT_TOKEN_TYPES = dict(comprehension(SYMBOLS_TOKEN_MAP.items(), func(token, symbol) => (symbol, token)))

locals().update(TOKENS)

__all__ = (
    'TOKENS',
    'SYMBOLS_TOKEN_MAP',
    'EXACT_TOKEN_TYPES'
) + tuple(TOKENS.keys())