from parser import TOKENS
from '_pyscript>mapping' import SYMBOLS_TOKEN_MAP

EXACT_TOKEN_TYPES = dict(comprehension(SYMBOLS_TOKEN_MAP.items(), func(i) => (i[1], i[0])))

locals().update(TOKENS)

__all__ = (
    'SYMBOLS_TOKEN_MAP',
    'EXACT_TOKEN_TYPES',
    'TOKENS'
) + tuple(TOKENS.keys())